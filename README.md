# HTML Previewer

HTML 코드를 붙여넣고 렌더링된 화면에서 텍스트를 직접 수정한 뒤, 변경된 HTML을 다시 복사하거나 파일로 저장할 수 있는 웹 편집기입니다.

## 로컬 실행

```bash
npm install
npm run dev
```

## 배포

Vercel에서 이 저장소를 가져오면 Vite 프로젝트로 자동 감지됩니다.

- Build Command: `npm run build`
- Output Directory: `dist`

